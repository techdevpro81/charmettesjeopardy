# ElevenLabs TTS API key (for voice narration)
ELEVENLABS_API_KEY=your_elevenlabs_api_key

# Frontend: use HTTP polling API (required for Vercel serverless)
VITE_USE_HTTP_API=true

# Leave empty on Vercel (same-origin). Set to a URL for local cross-origin dev.
VITE_API_BASE_URL=
